import type { JsonObject, ToolAnnotations, ToolDefinitionSummary, ToolInput, ToolName } from "@mako-ai/contracts";
import {
  AskToolInputSchema,
  AskToolOutputSchema,
  AuthPathToolInputSchema,
  AuthPathToolOutputSchema,
  DbColumnsToolInputSchema,
  DbColumnsToolOutputSchema,
  DbFkToolInputSchema,
  DbFkToolOutputSchema,
  DbPingToolInputSchema,
  DbPingToolOutputSchema,
  DbRlsToolInputSchema,
  DbRlsToolOutputSchema,
  DbRpcToolInputSchema,
  DbRpcToolOutputSchema,
  DbTableSchemaToolInputSchema,
  DbTableSchemaToolOutputSchema,
  ExportsOfToolInputSchema,
  ExportsOfToolOutputSchema,
  FileHealthToolInputSchema,
  FileHealthToolOutputSchema,
  FlowMapToolInputSchema,
  FlowMapToolOutputSchema,
  HealthTrendToolInputSchema,
  HealthTrendToolOutputSchema,
  ImplementationHandoffArtifactToolInputSchema,
  ImplementationHandoffArtifactToolOutputSchema,
  ReviewBundleArtifactToolInputSchema,
  ReviewBundleArtifactToolOutputSchema,
  TaskPreflightArtifactToolInputSchema,
  TaskPreflightArtifactToolOutputSchema,
  VerificationBundleArtifactToolInputSchema,
  VerificationBundleArtifactToolOutputSchema,
  IssuesNextToolInputSchema,
  IssuesNextToolOutputSchema,
  SessionHandoffToolInputSchema,
  SessionHandoffToolOutputSchema,
  SuggestToolInputSchema,
  SuggestToolOutputSchema,
  GraphNeighborsToolInputSchema,
  GraphNeighborsToolOutputSchema,
  GraphPathToolInputSchema,
  GraphPathToolOutputSchema,
  InvestigateToolInputSchema,
  InvestigateToolOutputSchema,
  ImportsCyclesToolInputSchema,
  ImportsCyclesToolOutputSchema,
  ImportsDepsToolInputSchema,
  ImportsDepsToolOutputSchema,
  ImportsHotspotsToolInputSchema,
  ImportsHotspotsToolOutputSchema,
  ImportsImpactToolInputSchema,
  ImportsImpactToolOutputSchema,
  RouteTraceToolInputSchema,
  RouteTraceToolOutputSchema,
  SchemaUsageToolInputSchema,
  SchemaUsageToolOutputSchema,
  SymbolsOfToolInputSchema,
  SymbolsOfToolOutputSchema,
  TenantLeakAuditToolInputSchema,
  TenantLeakAuditToolOutputSchema,
  ToolAnnotationsSchema,
  TraceFileToolInputSchema,
  TraceFileToolOutputSchema,
  WorkflowPacketToolInputSchema,
  WorkflowPacketToolOutputSchema,
  ChangePlanToolInputSchema,
  ChangePlanToolOutputSchema,
  AstFindPatternToolInputSchema,
  AstFindPatternToolOutputSchema,
  LiveTextSearchToolInputSchema,
  LiveTextSearchToolOutputSchema,
  LintFilesToolInputSchema,
  LintFilesToolOutputSchema,
  BiomeDiagnosticsToolInputSchema,
  BiomeDiagnosticsToolOutputSchema,
  EslintDiagnosticsToolInputSchema,
  EslintDiagnosticsToolOutputSchema,
  OxlintDiagnosticsToolInputSchema,
  OxlintDiagnosticsToolOutputSchema,
  TypeScriptDiagnosticsToolInputSchema,
  TypeScriptDiagnosticsToolOutputSchema,
  GitPrecommitCheckToolInputSchema,
  GitPrecommitCheckToolOutputSchema,
  RepoMapToolInputSchema,
  RepoMapToolOutputSchema,
  ContextPacketToolInputSchema,
  ContextPacketToolOutputSchema,
  ToolBatchInputSchema,
  ToolBatchToolOutputSchema,
  ProjectIndexStatusToolInputSchema,
  ProjectIndexStatusToolOutputSchema,
  ProjectIndexRefreshToolInputSchema,
  ProjectIndexRefreshToolOutputSchema,
  RuntimeTelemetryReportToolInputSchema,
  RuntimeTelemetryReportToolOutputSchema,
  AgentFeedbackToolInputSchema,
  AgentFeedbackToolOutputSchema,
  AgentFeedbackReportToolInputSchema,
  AgentFeedbackReportToolOutputSchema,
  RecallAnswersToolInputSchema,
  RecallAnswersToolOutputSchema,
  RecallToolRunsToolInputSchema,
  RecallToolRunsToolOutputSchema,
  RouteContextToolInputSchema,
  RouteContextToolOutputSchema,
  RpcNeighborhoodToolInputSchema,
  RpcNeighborhoodToolOutputSchema,
  TableNeighborhoodToolInputSchema,
  TableNeighborhoodToolOutputSchema,
  FindingAckToolInputSchema,
  FindingAckToolOutputSchema,
  FindingAckBatchToolInputSchema,
  FindingAckBatchToolOutputSchema,
  FindingAcksReportToolInputSchema,
  FindingAcksReportToolOutputSchema,
  DiagnosticRefreshToolInputSchema,
  DiagnosticRefreshToolOutputSchema,
  DbReefRefreshToolInputSchema,
  DbReefRefreshToolOutputSchema,
  DbReviewCommentToolInputSchema,
  DbReviewCommentToolOutputSchema,
  DbReviewCommentsToolInputSchema,
  DbReviewCommentsToolOutputSchema,
  EvidenceConfidenceToolInputSchema,
  EvidenceConfidenceToolOutputSchema,
  EvidenceConflictsToolInputSchema,
  EvidenceConflictsToolOutputSchema,
  FileFactsToolInputSchema,
  FileFactsToolOutputSchema,
  FileFindingsToolInputSchema,
  FileFindingsToolOutputSchema,
  ListReefRulesToolInputSchema,
  ListReefRulesToolOutputSchema,
  ProjectConventionsToolInputSchema,
  ProjectConventionsToolOutputSchema,
  ProjectDiagnosticRunsToolInputSchema,
  ProjectDiagnosticRunsToolOutputSchema,
  ProjectFactsToolInputSchema,
  ProjectFactsToolOutputSchema,
  ProjectFindingsToolInputSchema,
  ProjectFindingsToolOutputSchema,
  ProjectOpenLoopsToolInputSchema,
  ProjectOpenLoopsToolOutputSchema,
  ReefInstructionsToolInputSchema,
  ReefInstructionsToolOutputSchema,
  ReefOverlayDiffToolInputSchema,
  ReefOverlayDiffToolOutputSchema,
  ReefInspectToolInputSchema,
  ReefInspectToolOutputSchema,
  ReefScoutToolInputSchema,
  ReefScoutToolOutputSchema,
  RulePackValidateToolInputSchema,
  RulePackValidateToolOutputSchema,
  RuleMemoryToolInputSchema,
  RuleMemoryToolOutputSchema,
  VerificationStateToolInputSchema,
  VerificationStateToolOutputSchema,
  WorkingTreeOverlayToolInputSchema,
  WorkingTreeOverlayToolOutputSchema,
} from "@mako-ai/contracts";
import type { ZodTypeAny } from "zod";
import { zodToJsonSchema } from "zod-to-json-schema";
import { askTool } from "./ask/index.js";
import { agentFeedbackReportTool, agentFeedbackTool } from "./agent-feedback/index.js";
import { authPathTool, fileHealthTool, routeTraceTool, schemaUsageTool } from "./answers/index.js";
import {
  implementationHandoffArtifactTool,
  reviewBundleArtifactTool,
  taskPreflightArtifactTool,
  verificationBundleArtifactTool,
} from "./artifacts/index.js";
import {
  crossSearchTool,
  preflightTableTool,
  traceEdgeTool,
  traceErrorTool,
  traceFileTool,
  traceRpcTool,
  traceTableTool,
} from "./composers/index.js";
import {
  dbColumnsTool,
  dbFkTool,
  dbPingTool,
  dbRlsTool,
  dbRpcTool,
  dbTableSchemaTool,
} from "./db/index.js";
import { astFindPatternTool, biomeDiagnosticsTool, eslintDiagnosticsTool, gitPrecommitCheckTool, lintFilesTool, oxlintDiagnosticsTool, repoMapTool, typescriptDiagnosticsTool } from "./code-intel/index.js";
import { contextPacketTool } from "./context-packet/index.js";
import { liveTextSearchTool } from "./live-text-search/index.js";
import { projectIndexRefreshTool, projectIndexStatusTool } from "./project-index/index.js";
import {
  diagnosticRefreshTool,
  dbReviewCommentTool,
  dbReviewCommentsTool,
  dbReefRefreshTool,
  evidenceConfidenceTool,
  evidenceConflictsTool,
  fileFactsTool,
  fileFindingsTool,
  listReefRulesTool,
  projectConventionsTool,
  projectDiagnosticRunsTool,
  projectFactsTool,
  projectFindingsTool,
  projectOpenLoopsTool,
  reefInstructionsTool,
  reefOverlayDiffTool,
  reefInspectTool,
  reefScoutTool,
  ruleMemoryTool,
  verificationStateTool,
  workingTreeOverlayTool,
} from "./reef/index.js";
import { rulePackValidateTool } from "./rule-packs/index.js";
import { findingAckBatchTool, findingAckTool, findingAcksReportTool } from "./finding-acks/index.js";
import { runtimeTelemetryReportTool } from "./runtime-telemetry/index.js";
import { recallAnswersTool, recallToolRunsTool } from "./session-recall/index.js";
import { rpcNeighborhoodTool, routeContextTool, tableNeighborhoodTool } from "./neighborhoods/index.js";
import { changePlanTool, flowMapTool, graphNeighborsTool, graphPathTool } from "./graph/index.js";
import { importsCyclesTool, importsDepsTool, importsHotspotsTool, importsImpactTool } from "./imports/index.js";
import { investigateTool, suggestTool } from "./investigation/index.js";
import { tenantLeakAuditTool } from "./operators/index.js";
import { healthTrendTool, issuesNextTool, sessionHandoffTool } from "./project-intelligence/index.js";
import type { ToolServiceOptions } from "./runtime.js";
import { exportsOfTool, symbolsOfTool } from "./symbols/index.js";
import { toolBatchTool } from "./tool-batch/index.js";
import { workflowPacketTool } from "./workflow-packets/surfaces.js";

export interface MakoToolDefinition<Name extends string = ToolName> {
  name: Name;
  category: ToolDefinitionSummary["category"];
  description: string;
  annotations: ToolAnnotations;
  inputSchema: ZodTypeAny;
  outputSchema: ZodTypeAny;
  execute(input: ToolInput, options: ToolServiceOptions): Promise<unknown>;
}

export function schemaToJson(
  schema: ZodTypeAny,
  name: string,
  options: { useRootRefs?: boolean } = {},
): JsonObject {
  if (options.useRootRefs) {
    return zodToJsonSchema(schema, {
      name,
      $refStrategy: "root",
    }) as JsonObject;
  }

  return zodToJsonSchema(schema, {
    $refStrategy: "none",
  }) as JsonObject;
}

export const TOOL_DEFINITIONS: readonly MakoToolDefinition[] = [
  {
    name: "task_preflight_artifact",
    category: "artifact",
    description: "Artifact tool for start-of-work preparation: compose implementation brief, verification plan, change plan, and flow map into one typed preflight artifact. Basis is stable — identical inputs produce the same artifactId, so callers may dedupe on it.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: TaskPreflightArtifactToolInputSchema,
    outputSchema: TaskPreflightArtifactToolOutputSchema,
    execute: (input, options) => taskPreflightArtifactTool(input as never, options),
  },
  {
    name: "implementation_handoff_artifact",
    category: "artifact",
    description: "Artifact tool for reusable implementation handoff: compose one implementation brief and the current session handoff into a typed handoff artifact. Basis is session-scoped — session_handoff shifts with every tool call, so artifactId moves between calls by design (each call captures the current session state). Do not dedupe on artifactId.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: ImplementationHandoffArtifactToolInputSchema,
    outputSchema: ImplementationHandoffArtifactToolOutputSchema,
    execute: (input, options) => implementationHandoffArtifactTool(input as never, options),
  },
  {
    name: "review_bundle_artifact",
    category: "artifact",
    description: "Artifact tool for reviewer-facing change inspection: compose implementation brief, change plan, optional flow map, and optional tenant audit into one typed review bundle. Basis is stable — identical inputs produce the same artifactId, so callers may dedupe on it.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: ReviewBundleArtifactToolInputSchema,
    outputSchema: ReviewBundleArtifactToolOutputSchema,
    execute: (input, options) => reviewBundleArtifactTool(input as never, options),
  },
  {
    name: "verification_bundle_artifact",
    category: "artifact",
    description: "Artifact tool for safe completion and verification: compose verification plan, project-intelligence signals, and optional tenant audit into one typed verification bundle. Basis is stable by default; artifactId shifts across calls only when `includeSessionHandoff` or `includeIssuesNext` is set, because those inputs are session-scoped. Dedupe is safe when both flags are off.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: VerificationBundleArtifactToolInputSchema,
    outputSchema: VerificationBundleArtifactToolOutputSchema,
    execute: (input, options) => verificationBundleArtifactTool(input as never, options),
  },
  {
    name: "suggest",
    category: "workflow",
    description: "Workflow tool for tool-chain recommendations: choose one canonical workflow or a short ordered sequence without executing hidden planner logic.",
    annotations: { readOnlyHint: true, advisoryOnly: true, derivedOnly: true },
    inputSchema: SuggestToolInputSchema,
    outputSchema: SuggestToolOutputSchema,
    execute: (input, options) => suggestTool(input as never, options),
  },
  {
    name: "investigate",
    category: "workflow",
    description: "Workflow tool for bounded read-only investigation: run a short sequential tool chain over shipped workflows and return typed step history.",
    annotations: { readOnlyHint: true, advisoryOnly: true, derivedOnly: true },
    inputSchema: InvestigateToolInputSchema,
    outputSchema: InvestigateToolOutputSchema,
    execute: (input, options) => investigateTool(input as never, options),
  },
  {
    name: "graph_neighbors",
    category: "graph",
    description: "Graph tool for adjacency questions: traverse outward from one or more start entities with explicit direction, depth, and edge filters.",
    annotations: { readOnlyHint: true },
    inputSchema: GraphNeighborsToolInputSchema,
    outputSchema: GraphNeighborsToolOutputSchema,
    execute: (input, options) => graphNeighborsTool(input as never, options),
  },
  {
    name: "graph_path",
    category: "graph",
    description: "Graph tool for connection questions: find one shortest typed path between two entities while keeping heuristic edges explicit and opt-in.",
    annotations: { readOnlyHint: true },
    inputSchema: GraphPathToolInputSchema,
    outputSchema: GraphPathToolOutputSchema,
    execute: (input, options) => graphPathTool(input as never, options),
  },
  {
    name: "flow_map",
    category: "graph",
    description: "Graph tool for end-to-end flow questions: turn one graph path into ordered flow steps, transitions, and major boundary crossings.",
    annotations: { readOnlyHint: true },
    inputSchema: FlowMapToolInputSchema,
    outputSchema: FlowMapToolOutputSchema,
    execute: (input, options) => flowMapTool(input as never, options),
  },
  {
    name: "change_plan",
    category: "graph",
    description: "Graph tool for bounded change scope: return path-derived direct surfaces, one-hop dependent surfaces, and an explicit dependency order.",
    annotations: { readOnlyHint: true },
    inputSchema: ChangePlanToolInputSchema,
    outputSchema: ChangePlanToolOutputSchema,
    execute: (input, options) => changePlanTool(input as never, options),
  },
  {
    name: "tenant_leak_audit",
    category: "operator",
    description: "Operator tool for tenant-boundary review: audit tenant-keyed tables, RLS posture, and RPC/code touch points without making generic security-score claims.",
    annotations: { readOnlyHint: true, advisoryOnly: true, derivedOnly: true },
    inputSchema: TenantLeakAuditToolInputSchema,
    outputSchema: TenantLeakAuditToolOutputSchema,
    execute: (input, options) => tenantLeakAuditTool(input as never, options),
  },
  {
    name: "session_handoff",
    category: "operator",
    description: "Operator tool for derived project-state handoff: summarize recent answer traces, active unresolved focus, and recorded follow-up momentum.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: SessionHandoffToolInputSchema,
    outputSchema: SessionHandoffToolOutputSchema,
    execute: (input, options) => sessionHandoffTool(input as never, options),
  },
  {
    name: "recall_answers",
    category: "session_recall",
    description: "Read-only session-recall tool: search prior project answer traces by text, query kind, support level, trust state, and ISO time window. Returns bounded answer summaries, pre-cap matchCount, truncation signal, and optional stored answer markdown. Does not run the answer loop or infer freshness.",
    annotations: { readOnlyHint: true },
    inputSchema: RecallAnswersToolInputSchema,
    outputSchema: RecallAnswersToolOutputSchema,
    execute: (input, options) => recallAnswersTool(input as never, options),
  },
  {
    name: "recall_tool_runs",
    category: "session_recall",
    description: "Read-only session-recall tool: inspect prior project tool_runs by tool name, outcome, request id, and ISO time window. Returns bounded summaries with runId plus requestId when available; use requestId as agent_feedback.referencedRequestId. Default limit is 50, max 500. Persisted payloads are only included when includePayload is true.",
    annotations: { readOnlyHint: true },
    inputSchema: RecallToolRunsToolInputSchema,
    outputSchema: RecallToolRunsToolOutputSchema,
    execute: (input, options) => recallToolRunsTool(input as never, options),
  },
  {
    name: "table_neighborhood",
    category: "neighborhood",
    description: "Neighborhood composer for one table: combines db_table_schema/db_rls-equivalent schema snapshot data, schema_usage read/write sites, trace_table RPC-to-table edges, and route_trace handler routes into one bounded typed bundle.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: TableNeighborhoodToolInputSchema,
    outputSchema: TableNeighborhoodToolOutputSchema,
    execute: (input, options) => tableNeighborhoodTool(input as never, options),
  },
  {
    name: "route_context",
    category: "neighborhood",
    description: "Neighborhood composer for one route: combines route_trace route resolution, file_health handler summary, imports_deps direct imports/dependents, schema_usage touches, trace_rpc downstream table edges, and db_rls policy surfaces.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: RouteContextToolInputSchema,
    outputSchema: RouteContextToolOutputSchema,
    execute: (input, options) => routeContextTool(input as never, options),
  },
  {
    name: "rpc_neighborhood",
    category: "neighborhood",
    description: "Neighborhood composer for one RPC: combines db_rpc-equivalent schema snapshot signature/body, schema_usage app-code callers, trace_rpc function-to-table refs, and db_rls policies on touched tables into one bounded bundle.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: RpcNeighborhoodToolInputSchema,
    outputSchema: RpcNeighborhoodToolOutputSchema,
    execute: (input, options) => rpcNeighborhoodTool(input as never, options),
  },
  {
    name: "agent_feedback",
    category: "feedback",
    description: "Append-only feedback tool for rating a prior Mako tool run from the agent perspective. Use sparingly when a result was notably good, notably bad, or wrong; do not emit routine feedback after every call. Requires referencedToolName and referencedRequestId so each row is tied to a specific prior run. Grade semantics: full = helped complete the task, partial = somewhat useful but flawed, no = wrong or wasted the turn. Starter reason codes: full: answer_complete, evidence_sufficient, trust_matches; partial: partial_coverage, noisy, stale_evidence, missing_known_caller, top_not_useful; no: answer_wrong, wasted_turn, tool_did_nothing, schema_missing.",
    annotations: { mutation: true },
    inputSchema: AgentFeedbackToolInputSchema,
    outputSchema: AgentFeedbackToolOutputSchema,
    execute: (input, options) => agentFeedbackTool(input as never, options),
  },
  {
    name: "agent_feedback_report",
    category: "feedback",
    description: "Read-only inspection over agent_feedback rows in mako_usefulness_events: return grade counts by referenced tool plus bounded recent entries, filterable by referencedToolName, grade, and ISO time window. Use to review which tools agents found helpful, noisy, wrong, or incomplete.",
    annotations: { readOnlyHint: true },
    inputSchema: AgentFeedbackReportToolInputSchema,
    outputSchema: AgentFeedbackReportToolOutputSchema,
    execute: (input, options) => agentFeedbackReportTool(input as never, options),
  },
  {
    name: "health_trend",
    category: "operator",
    description: "Operator tool for project-state trend review: compare a recent trace window against a prior window without fabricating trend lines when history is thin.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: HealthTrendToolInputSchema,
    outputSchema: HealthTrendToolOutputSchema,
    execute: (input, options) => healthTrendTool(input as never, options),
  },
  {
    name: "issues_next",
    category: "operator",
    description: "Operator tool for queue-oriented recommendations: derive one current issue plus queued follow-on issues from recent unresolved project traces.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: IssuesNextToolInputSchema,
    outputSchema: IssuesNextToolOutputSchema,
    execute: (input, options) => issuesNextTool(input as never, options),
  },
  {
    name: "route_trace",
    category: "answer",
    description: "Answer tool for route questions: trace a route to the indexed handler, matching files, and nearby evidence.",
    annotations: { readOnlyHint: true },
    inputSchema: RouteTraceToolInputSchema,
    outputSchema: RouteTraceToolOutputSchema,
    execute: (input, options) => routeTraceTool(input as never, options),
  },
  {
    name: "schema_usage",
    category: "answer",
    description: "Answer tool for schema questions: find where an indexed schema object is defined and referenced in the repo.",
    annotations: { readOnlyHint: true },
    inputSchema: SchemaUsageToolInputSchema,
    outputSchema: SchemaUsageToolOutputSchema,
    execute: (input, options) => schemaUsageTool(input as never, options),
  },
  {
    name: "file_health",
    category: "answer",
    description: "Answer tool for file questions: summarize a file's role, dependents, and notable risks with evidence.",
    annotations: { readOnlyHint: true },
    inputSchema: FileHealthToolInputSchema,
    outputSchema: FileHealthToolOutputSchema,
    execute: (input, options) => fileHealthTool(input as never, options),
  },
  {
    name: "auth_path",
    category: "answer",
    description: "Answer tool for auth questions: trace likely auth boundaries for a route, file, or feature without overclaiming.",
    annotations: { readOnlyHint: true },
    inputSchema: AuthPathToolInputSchema,
    outputSchema: AuthPathToolOutputSchema,
    execute: (input, options) => authPathTool(input as never, options),
  },
  {
    name: "imports_deps",
    category: "imports",
    description: "Imports tool for direct dependencies: list a file's indexed imports and flag unresolved internal edges.",
    annotations: { readOnlyHint: true },
    inputSchema: ImportsDepsToolInputSchema,
    outputSchema: ImportsDepsToolOutputSchema,
    execute: (input, options) => importsDepsTool(input as never, options),
  },
  {
    name: "imports_impact",
    category: "imports",
    description: "Imports tool for downstream impact: trace which indexed files depend on a file through the import graph.",
    annotations: { readOnlyHint: true },
    inputSchema: ImportsImpactToolInputSchema,
    outputSchema: ImportsImpactToolOutputSchema,
    execute: (input, options) => importsImpactTool(input as never, options),
  },
  {
    name: "imports_hotspots",
    category: "imports",
    description: "Imports tool for graph hotspots: rank the most connected files in the indexed internal import graph.",
    annotations: { readOnlyHint: true },
    inputSchema: ImportsHotspotsToolInputSchema,
    outputSchema: ImportsHotspotsToolOutputSchema,
    execute: (input, options) => importsHotspotsTool(input as never, options),
  },
  {
    name: "imports_cycles",
    category: "imports",
    description: "Imports tool for cycle detection: detect circular dependencies in the indexed internal import graph.",
    annotations: { readOnlyHint: true },
    inputSchema: ImportsCyclesToolInputSchema,
    outputSchema: ImportsCyclesToolOutputSchema,
    execute: (input, options) => importsCyclesTool(input as never, options),
  },
  {
    name: "symbols_of",
    category: "symbols",
    description: "Symbols tool for declarations: list the indexed symbols declared in a file.",
    annotations: { readOnlyHint: true },
    inputSchema: SymbolsOfToolInputSchema,
    outputSchema: SymbolsOfToolOutputSchema,
    execute: (input, options) => symbolsOfTool(input as never, options),
  },
  {
    name: "exports_of",
    category: "symbols",
    description: "Symbols tool for exports: list only the indexed symbols that a file exports.",
    annotations: { readOnlyHint: true },
    inputSchema: ExportsOfToolInputSchema,
    outputSchema: ExportsOfToolOutputSchema,
    execute: (input, options) => exportsOfTool(input as never, options),
  },
  {
    name: "ask",
    category: "router",
    description: "Router tool: map one natural-language engineering question to one canonical named tool, or conservatively fall back to free_form.",
    annotations: { readOnlyHint: true },
    inputSchema: AskToolInputSchema,
    outputSchema: AskToolOutputSchema,
    execute: (input, options) => askTool(input as never, options),
  },
  {
    name: "workflow_packet",
    category: "workflow",
    description: "Workflow tool: generate a typed workflow packet from a project-scoped query answer and expose watch/surface metadata.",
    annotations: { readOnlyHint: true },
    inputSchema: WorkflowPacketToolInputSchema,
    outputSchema: WorkflowPacketToolOutputSchema,
    execute: (input, options) => workflowPacketTool(input as never, options),
  },
  {
    name: "ast_find_pattern",
    category: "code_intel",
    description: "Code-intelligence tool for structural pattern search: run one ast-grep pattern against every fresh indexed TypeScript / TSX / JavaScript / JSX file and return typed matches (file, line range, column range, matched text, captured metavariables). Reef-backed freshness guard skips stale/deleted/unknown indexed files so old AST rows do not become phantom matches; set MAKO_REEF_BACKED=legacy for the one-release rollback path. Read-only; never edits files. Use when text/FTS search is too noisy for structural queries (e.g. `console.log($X)`, `await supabase.rpc($NAME)`, `useEffect($FN, [])`).",
    annotations: { readOnlyHint: true },
    inputSchema: AstFindPatternToolInputSchema,
    outputSchema: AstFindPatternToolOutputSchema,
    execute: (input, options) => astFindPatternTool(input as never, options),
  },
  {
    name: "live_text_search",
    category: "code_intel",
    description: "Code-intelligence tool for live filesystem text search: run ripgrep over the attached project root and return bounded file/line matches. Defaults to fixed-string matching; pass fixedStrings: false to use ripgrep regex syntax. Read-only and explicitly live, so use it when the indexed snapshot may be stale or when exact text search is the right primitive.",
    annotations: { readOnlyHint: true },
    inputSchema: LiveTextSearchToolInputSchema,
    outputSchema: LiveTextSearchToolOutputSchema,
    execute: (input, options) => liveTextSearchTool(input as never, options),
  },
  {
    name: "lint_files",
    category: "code_intel",
    description: "Code-intelligence tool for diagnostics on an indexed file set: run rule-packs + TS-aware and structural alignment diagnostics, persist indexed Reef findings/run metadata under source `lint_files`, and return typed AnswerSurfaceIssue findings. Same engine that powers the answer loop and review_bundle, so findings stay consistent across surfaces.",
    annotations: { mutation: true, advisoryOnly: true },
    inputSchema: LintFilesToolInputSchema,
    outputSchema: LintFilesToolOutputSchema,
    execute: (input, options) => lintFilesTool(input as never, options),
  },
  {
    name: "typescript_diagnostics",
    category: "code_intel",
    description: "Explicit Reef ingestion tool for TypeScript compiler diagnostics: read tsconfig, run the TypeScript program with no emit, persist working-tree ProjectFinding rows under source `typescript`, and return bounded findings. This may run a broad typecheck, so call it only when operator intent justifies the cost.",
    annotations: { mutation: true, advisoryOnly: true },
    inputSchema: TypeScriptDiagnosticsToolInputSchema,
    outputSchema: TypeScriptDiagnosticsToolOutputSchema,
    execute: (input, options) => typescriptDiagnosticsTool(input as never, options),
  },
  {
    name: "eslint_diagnostics",
    category: "code_intel",
    description: "Explicit Reef ingestion tool for ESLint diagnostics: run the project's local ESLint executable on the requested project-relative files with JSON output, persist working-tree ProjectFinding rows under source `eslint`, and record unavailable/error/succeeded run status. File-mode only; does not run broad project lint.",
    annotations: { mutation: true, advisoryOnly: true },
    inputSchema: EslintDiagnosticsToolInputSchema,
    outputSchema: EslintDiagnosticsToolOutputSchema,
    execute: (input, options) => eslintDiagnosticsTool(input as never, options),
  },
  {
    name: "oxlint_diagnostics",
    category: "code_intel",
    description: "Explicit Reef ingestion tool for Oxlint diagnostics: run the project's local Oxlint executable or JSON package script on requested project-relative files with `--format json`, persist working-tree ProjectFinding rows under source `oxlint`, and record unavailable/error/succeeded run status. File-mode only; does not run broad project lint.",
    annotations: { mutation: true, advisoryOnly: true },
    inputSchema: OxlintDiagnosticsToolInputSchema,
    outputSchema: OxlintDiagnosticsToolOutputSchema,
    execute: (input, options) => oxlintDiagnosticsTool(input as never, options),
  },
  {
    name: "biome_diagnostics",
    category: "code_intel",
    description: "Explicit Reef ingestion tool for Biome diagnostics: run the project's local Biome executable or GitLab-reporter package script on requested project-relative files with `check --reporter=gitlab`, persist working-tree ProjectFinding rows under source `biome`, and record unavailable/error/succeeded run status. File-mode only; deliberately avoids Biome's experimental JSON reporter.",
    annotations: { mutation: true, advisoryOnly: true },
    inputSchema: BiomeDiagnosticsToolInputSchema,
    outputSchema: BiomeDiagnosticsToolOutputSchema,
    execute: (input, options) => biomeDiagnosticsTool(input as never, options),
  },
  {
    name: "git_precommit_check",
    category: "code_intel",
    description: "Git pre-commit guard for staged TS/TSX files: reads staged blobs, checks API route auth guards and Next.js server/client boundary mistakes, persists staged Reef findings unless MAKO_REEF_BACKED disables the migration, and returns hook-friendly continue/stopReason output. Uses discovered project-profile auth guards/server-only modules plus optional .mako/git-guard.json/input allowlists. Never edits the index or worktree.",
    annotations: { mutation: true, advisoryOnly: true },
    inputSchema: GitPrecommitCheckToolInputSchema,
    outputSchema: GitPrecommitCheckToolOutputSchema,
    execute: (input, options) => gitPrecommitCheckTool(input as never, options),
  },
  {
    name: "diagnostic_refresh",
    category: "code_intel",
    description: "Explicit Reef diagnostic ingestion runner: invoke selected diagnostic sources (lint_files, TypeScript, ESLint, Oxlint, Biome, or git_precommit_check), persist their Reef diagnostic runs/findings through the underlying tools, and return a compact per-source summary. File-mode sources are skipped unless files are supplied; this is a mutation tool because it refreshes Reef state.",
    annotations: { mutation: true, advisoryOnly: true },
    inputSchema: DiagnosticRefreshToolInputSchema,
    outputSchema: DiagnosticRefreshToolOutputSchema,
    execute: (input, options) => diagnosticRefreshTool(input as never, options),
  },
  {
    name: "db_reef_refresh",
    category: "project",
    description: "Explicit Reef DB ingestion tool: replace indexed Reef facts for the current schema snapshot, including schemas, tables, columns, indexes, foreign keys, RLS policies, triggers, enums, RPCs, RPC-to-table refs, and indexed schema usages. Uses the existing schema snapshot/read-model indexes; run project_index_refresh first if the snapshot is stale or missing.",
    annotations: { mutation: true, advisoryOnly: true },
    inputSchema: DbReefRefreshToolInputSchema,
    outputSchema: DbReefRefreshToolOutputSchema,
    execute: (input, options) => dbReefRefreshTool(input as never, options),
  },
  {
    name: "db_review_comment",
    category: "project",
    description: "Append-only DB review note tool: leave a short AI/operator comment on a database object such as a table, column, policy, trigger, publication, replication slot, or general replication topic. Writes only to Mako's local project store; it never mutates the live database.",
    annotations: { mutation: true, advisoryOnly: true },
    inputSchema: DbReviewCommentToolInputSchema,
    outputSchema: DbReviewCommentToolOutputSchema,
    execute: (input, options) => dbReviewCommentTool(input as never, options),
  },
  {
    name: "db_review_comments",
    category: "project",
    description: "Read DB review comments from Mako's local append-only ledger, filterable by database object, category, tag, free-text query, or target fingerprint. Use during schema/table/RLS/replication review to recover prior AI/operator notes.",
    annotations: { readOnlyHint: true },
    inputSchema: DbReviewCommentsToolInputSchema,
    outputSchema: DbReviewCommentsToolOutputSchema,
    execute: (input, options) => dbReviewCommentsTool(input as never, options),
  },
  {
    name: "repo_map",
    category: "code_intel",
    description: "Code-intelligence tool for repo orientation: emit a token-budgeted aider-style outline of the indexed project (ranked files + key symbols) as first-turn context for agents meeting an unfamiliar codebase. Ranking approximates centrality via `fanIn * 2 + fanOut + 0.1`; symbol selection prefers exported declarations. Read-only; default budget 1024 tokens (char/4 approximation), cap 16384.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: RepoMapToolInputSchema,
    outputSchema: RepoMapToolOutputSchema,
    execute: (input, options) => repoMapTool(input as never, options),
  },
  {
    name: "context_packet",
    category: "context",
    description: "Context scout for coding agents: turn a messy request into ranked, source-labeled, readable context using deterministic providers first (files, routes, symbols, schema, import graph, centrality, hot hints). Reef-backed enrichments add working-tree overlay metadata and active findings; set MAKO_REEF_BACKED=legacy for the one-release rollback path. Read-only; does not refresh the index. Use as the first-mile packet before normal harness read/search/edit loops.",
    annotations: { readOnlyHint: true, derivedOnly: true, advisoryOnly: true },
    inputSchema: ContextPacketToolInputSchema,
    outputSchema: ContextPacketToolOutputSchema,
    execute: (input, options) => contextPacketTool(input as never, options),
  },
  {
    name: "tool_batch",
    category: "context",
    description: "Read-only batching wrapper for independent Mako lookups. The input schema only accepts batchable read-only tools; runtime also rejects mutation tools and recursive tool_batch calls defensively. Returns labeled sub-results with per-op duration. Use `verbosity: \"compact\"` or per-op `resultMode: \"summary\"` to return compact summaries instead of full payloads; per-op `resultMode: \"full\"` keeps the full output. Each sub-op's `projectId` is overridden with the parent batch's resolved project, so all ops run against one project. Use to reduce round-trips after a context_packet recommends several expansions.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: ToolBatchInputSchema,
    outputSchema: ToolBatchToolOutputSchema,
    execute: (input, options) => toolBatchTool(input as never, options),
  },
  {
    name: "project_index_status",
    category: "project",
    description: "Project tool for index freshness: compare indexed file rows against live disk metadata, report Reef working-tree fact freshness, watch state, latest index run, unindexedScan status, and a suggested next action. By default this checks indexed rows only and reports that the new-file scan was skipped; pass includeUnindexed: true to pay the repo walk cost and get an exact count/details for files not yet in the index. Trust/stability is not freshness; this tool answers whether the index snapshot is current.",
    annotations: { readOnlyHint: true, advisoryOnly: true },
    inputSchema: ProjectIndexStatusToolInputSchema,
    outputSchema: ProjectIndexStatusToolOutputSchema,
    execute: (input, options) => projectIndexStatusTool(input as never, options),
  },
  {
    name: "project_index_refresh",
    category: "project",
    description: "Mutation tool for index freshness: run the project indexer through the current project-store cache when the snapshot is stale, unknown, deleted, or has unindexed files, then return before/after freshness summaries and the new index run. In if_stale mode, unknown freshness refreshes defensively and unindexed files count as stale work.",
    annotations: { mutation: true },
    inputSchema: ProjectIndexRefreshToolInputSchema,
    outputSchema: ProjectIndexRefreshToolOutputSchema,
    execute: (input, options) => projectIndexRefreshTool(input as never, options),
  },
  {
    name: "runtime_telemetry_report",
    category: "runtime_telemetry",
    description: "Read-only inspection over mako_usefulness_events (Phase 8.1): return aggregate counts (by decisionKind, by family, by grade) and a bounded list of recent events, filterable by decisionKind, family, requestId, and ISO time window. Use to verify usefulness capture is flowing or to triage why a decision site emitted the grade it did. Never writes to the event table.",
    annotations: { readOnlyHint: true },
    inputSchema: RuntimeTelemetryReportToolInputSchema,
    outputSchema: RuntimeTelemetryReportToolOutputSchema,
    execute: (input, options) => runtimeTelemetryReportTool(input as never, options),
  },
  {
    name: "finding_ack",
    category: "finding_acks",
    description: "Mutation tool for verified-safe markers: append one row to the finding_acks ledger so subsequent ast_find_pattern / lint_files callers that opt into the same category filter the match out. Category is caller-owned (for lint findings, finding.code is the recommended default). For ast_find_pattern matches, pass `match.ackableFingerprint`; for lint_files findings, pass `finding.identity.matchBasedId`. Append-only; duplicate (projectId, category, fingerprint) inserts persist as separate rows and dedupe at query time. Emits one RuntimeUsefulnessEvent per successful call.",
    annotations: { mutation: true },
    inputSchema: FindingAckToolInputSchema,
    outputSchema: FindingAckToolOutputSchema,
    execute: (input, options) => findingAckTool(input as never, options),
  },
  {
    name: "finding_ack_batch",
    category: "finding_acks",
    description: "Mutation batch for reviewed finding acknowledgements: append many finding_acks rows in one call while sharing batch-level defaults for category, subjectKind, status, reason, acknowledgedBy, sourceToolName, and sourceRuleId. Use when a scan returns many reviewed false positives or accepted tradeoffs. Emits one finding_ack telemetry event per successful row.",
    annotations: { mutation: true },
    inputSchema: FindingAckBatchToolInputSchema,
    outputSchema: FindingAckBatchToolOutputSchema,
    execute: (input, options) => findingAckBatchTool(input as never, options),
  },
  {
    name: "finding_acks_report",
    category: "finding_acks",
    description: "Read-only inspection over the finding_acks ledger: return aggregate counts (by category, status, subjectKind, filePath) and a bounded reverse-chronological list of acks, filterable by category, subjectKind, filePath, status, and ISO time window. Use to see what operators have suppressed and which categories carry the most weight. Never writes to the ledger.",
    annotations: { readOnlyHint: true },
    inputSchema: FindingAcksReportToolInputSchema,
    outputSchema: FindingAcksReportToolOutputSchema,
    execute: (input, options) => findingAcksReportTool(input as never, options),
  },
  {
    name: "project_findings",
    category: "project",
    description: "Reef read tool for active project findings: query the durable Reef findings view by overlay, source, status, and resolved inclusion. Acknowledged status is derived from the existing finding_ack ledger, so this is the canonical read path for Reef-native lint/rule findings.",
    annotations: { readOnlyHint: true },
    inputSchema: ProjectFindingsToolInputSchema,
    outputSchema: ProjectFindingsToolOutputSchema,
    execute: (input, options) => projectFindingsTool(input as never, options),
  },
  {
    name: "file_findings",
    category: "project",
    description: "Reef read tool for one project-relative file: return durable Reef findings attached to that file, with the same overlay/source/status filters as project_findings. Use this before editing a file when a shell or agent needs known active diagnostics without rerunning broad lint.",
    annotations: { readOnlyHint: true },
    inputSchema: FileFindingsToolInputSchema,
    outputSchema: FileFindingsToolOutputSchema,
    execute: (input, options) => fileFindingsTool(input as never, options),
  },
  {
    name: "project_facts",
    category: "project",
    description: "Reef read tool for durable project facts: query facts by overlay, source, kind, and subject fingerprint. Use this when an agent or shell needs the calculated substrate behind findings without rerunning analysis.",
    annotations: { readOnlyHint: true },
    inputSchema: ProjectFactsToolInputSchema,
    outputSchema: ProjectFactsToolOutputSchema,
    execute: (input, options) => projectFactsTool(input as never, options),
  },
  {
    name: "file_facts",
    category: "project",
    description: "Reef read tool for one file's durable facts: computes the file subject fingerprint and returns facts such as working_tree_overlay file_snapshot rows. Use this to inspect what Reef currently knows about a file across overlays.",
    annotations: { readOnlyHint: true },
    inputSchema: FileFactsToolInputSchema,
    outputSchema: FileFactsToolOutputSchema,
    execute: (input, options) => fileFactsTool(input as never, options),
  },
  {
    name: "working_tree_overlay",
    category: "project",
    description: "Advisory Reef mutation: snapshot working-tree file facts for explicit files, watcher-dirty paths, or non-fresh indexed paths. Persists file_snapshot facts with size, mtime, line count, sha256, or deleted state; it does not reparse AST, imports, routes, or schema.",
    annotations: { mutation: true, advisoryOnly: true },
    inputSchema: WorkingTreeOverlayToolInputSchema,
    outputSchema: WorkingTreeOverlayToolOutputSchema,
    execute: (input, options) => workingTreeOverlayTool(input as never, options),
  },
  {
    name: "reef_overlay_diff",
    category: "project",
    description: "Reef read tool for overlay comparison: diff durable facts between two overlays, defaulting indexed vs working_tree, with optional filePath/kind/source filters. Use after working_tree_overlay or before edits to see exactly what Reef believes changed without rerunning diagnostics.",
    annotations: { readOnlyHint: true },
    inputSchema: ReefOverlayDiffToolInputSchema,
    outputSchema: ReefOverlayDiffToolOutputSchema,
    execute: (input, options) => reefOverlayDiffTool(input as never, options),
  },
  {
    name: "reef_instructions",
    category: "project",
    description: "Reef read tool for scoped project instructions: load .mako/instructions.md and applicable AGENTS.md files for requested project-relative files, returning structured instruction items plus derived Reef fact shapes. Reads only inside the project root and does not persist facts.",
    annotations: { readOnlyHint: true, derivedOnly: true },
    inputSchema: ReefInstructionsToolInputSchema,
    outputSchema: ReefInstructionsToolOutputSchema,
    execute: (input, options) => reefInstructionsTool(input as never, options),
  },
  {
    name: "list_reef_rules",
    category: "project",
    description: "Reef read tool for rule metadata: list durable rule descriptors, optionally filtered by source namespace or enabled-by-default status. Descriptors explain which facts a finding source consumes and what rule ids mean.",
    annotations: { readOnlyHint: true },
    inputSchema: ListReefRulesToolInputSchema,
    outputSchema: ListReefRulesToolOutputSchema,
    execute: (input, options) => listReefRulesTool(input as never, options),
  },
  {
    name: "rule_pack_validate",
    category: "project",
    description: "Read-only Reef rule-pack authoring tool: validate .mako/rules YAML packs, return pack/rule counts, schema errors, and optional ReefRuleDescriptor previews without running diagnostics or writing Reef state.",
    annotations: { readOnlyHint: true, advisoryOnly: true, derivedOnly: true },
    inputSchema: RulePackValidateToolInputSchema,
    outputSchema: RulePackValidateToolOutputSchema,
    execute: (input, options) => rulePackValidateTool(input as never, options),
  },
  {
    name: "project_diagnostic_runs",
    category: "project",
    description: "Reef read tool for diagnostic source runs: list recent lint/type adapter runs by source and status, including unavailable/error/succeeded state, duration, counts, command, config, cwd, metadata, and derived cache freshness/age. Use this to distinguish no findings from a diagnostic source that did not run or ran too long ago.",
    annotations: { readOnlyHint: true },
    inputSchema: ProjectDiagnosticRunsToolInputSchema,
    outputSchema: ProjectDiagnosticRunsToolOutputSchema,
    execute: (input, options) => projectDiagnosticRunsTool(input as never, options),
  },
  {
    name: "reef_scout",
    category: "context",
    description: "Reef 7 model-facing scout view: turn a messy request into ranked, explainable candidates from durable Reef facts, findings, rules, and diagnostic runs. This is a context scout, not an editing agent; use normal harness read/search after the packet.",
    annotations: { readOnlyHint: true },
    inputSchema: ReefScoutToolInputSchema,
    outputSchema: ReefScoutToolOutputSchema,
    execute: (input, options) => reefScoutTool(input as never, options),
  },
  {
    name: "reef_inspect",
    category: "context",
    description: "Reef 7 inspection view: explain one file or subject fingerprint by returning its durable facts, findings, and relevant diagnostic runs. Use after reef_scout when the model needs the evidence trail and freshness details before editing.",
    annotations: { readOnlyHint: true },
    inputSchema: ReefInspectToolInputSchema,
    outputSchema: ReefInspectToolOutputSchema,
    execute: (input, options) => reefInspectTool(input as never, options),
  },
  {
    name: "project_open_loops",
    category: "project",
    description: "Reef 8 open-loop view: list unresolved active findings, stale or unknown facts, and stale or failed diagnostic runs so an agent can see what still needs attention without rerunning broad checks.",
    annotations: { readOnlyHint: true },
    inputSchema: ProjectOpenLoopsToolInputSchema,
    outputSchema: ProjectOpenLoopsToolOutputSchema,
    execute: (input, options) => projectOpenLoopsTool(input as never, options),
  },
  {
    name: "verification_state",
    category: "project",
    description: "Reef 8 verification state view: summarize whether cached diagnostic runs still cover the current working-tree overlay, including files modified after successful checks and suggested verification actions.",
    annotations: { readOnlyHint: true },
    inputSchema: VerificationStateToolInputSchema,
    outputSchema: VerificationStateToolOutputSchema,
    execute: (input, options) => verificationStateTool(input as never, options),
  },
  {
    name: "project_conventions",
    category: "project",
    description: "Reef 9 convention view: surface explicit convention facts and rule-derived convention candidates such as auth guards, runtime boundaries, generated paths, route patterns, and schema usage conventions.",
    annotations: { readOnlyHint: true },
    inputSchema: ProjectConventionsToolInputSchema,
    outputSchema: ProjectConventionsToolOutputSchema,
    execute: (input, options) => projectConventionsTool(input as never, options),
  },
  {
    name: "rule_memory",
    category: "project",
    description: "Reef 9 rule-memory view: aggregate Reef rule descriptors and finding history so agents can see which rules are active, acknowledged, resolved, or suppressed before making changes.",
    annotations: { readOnlyHint: true },
    inputSchema: RuleMemoryToolInputSchema,
    outputSchema: RuleMemoryToolOutputSchema,
    execute: (input, options) => ruleMemoryTool(input as never, options),
  },
  {
    name: "evidence_confidence",
    category: "context",
    description: "Reef 10 evidence-confidence view: label facts and findings as verified_live, fresh_indexed, stale_indexed, fuzzy_semantic, historical, contradicted, or unknown so model-facing tools can prefer trustworthy evidence.",
    annotations: { readOnlyHint: true },
    inputSchema: EvidenceConfidenceToolInputSchema,
    outputSchema: EvidenceConfidenceToolOutputSchema,
    execute: (input, options) => evidenceConfidenceTool(input as never, options),
  },
  {
    name: "evidence_conflicts",
    category: "context",
    description: "Reef 10 conflict view: surface stale indexed evidence, explicit conflict facts, and findings that report incorrect or contradictory evidence, with suggested cross-check actions.",
    annotations: { readOnlyHint: true },
    inputSchema: EvidenceConflictsToolInputSchema,
    outputSchema: EvidenceConflictsToolOutputSchema,
    execute: (input, options) => evidenceConflictsTool(input as never, options),
  },
  {
    name: "db_ping",
    category: "db",
    description: "Read-only database tool: verify connectivity and surface platform, version, schemas, and transaction state.",
    annotations: { readOnlyHint: true },
    inputSchema: DbPingToolInputSchema,
    outputSchema: DbPingToolOutputSchema,
    execute: (input, options) => dbPingTool(input as never, options),
  },
  {
    name: "db_columns",
    category: "db",
    description: "Read-only database tool for column questions: inspect only columns and primary-key details for a table via pg_catalog.",
    annotations: { readOnlyHint: true },
    inputSchema: DbColumnsToolInputSchema,
    outputSchema: DbColumnsToolOutputSchema,
    execute: (input, options) => dbColumnsTool(input as never, options),
  },
  {
    name: "db_fk",
    category: "db",
    description: "Read-only database tool for relationship questions: inspect inbound and outbound foreign-key references for a table via pg_catalog.",
    annotations: { readOnlyHint: true },
    inputSchema: DbFkToolInputSchema,
    outputSchema: DbFkToolOutputSchema,
    execute: (input, options) => dbFkTool(input as never, options),
  },
  {
    name: "db_rls",
    category: "db",
    description: "Read-only database tool for security questions: inspect row-level security state and policies for a table via pg_catalog.",
    annotations: { readOnlyHint: true },
    inputSchema: DbRlsToolInputSchema,
    outputSchema: DbRlsToolOutputSchema,
    execute: (input, options) => dbRlsTool(input as never, options),
  },
  {
    name: "db_rpc",
    category: "db",
    description: "Read-only database tool for function questions: inspect one stored procedure or function signature, args, return shape, language, and security.",
    annotations: { readOnlyHint: true },
    inputSchema: DbRpcToolInputSchema,
    outputSchema: DbRpcToolOutputSchema,
    execute: (input, options) => dbRpcTool(input as never, options),
  },
  {
    name: "db_table_schema",
    category: "db",
    description: "Read-only database tool for full table shape: inspect columns, indexes, constraints, foreign keys, RLS, and triggers.",
    annotations: { readOnlyHint: true },
    inputSchema: DbTableSchemaToolInputSchema,
    outputSchema: DbTableSchemaToolOutputSchema,
    execute: (input, options) => dbTableSchemaTool(input as never, options),
  },
  traceFileTool,
  preflightTableTool,
  crossSearchTool,
  traceEdgeTool,
  traceErrorTool,
  traceTableTool,
  traceRpcTool,
] as const;

const TOOL_DEFINITION_MAP: Map<string, MakoToolDefinition<string>> = new Map();

for (const tool of TOOL_DEFINITIONS) {
  TOOL_DEFINITION_MAP.set(tool.name, tool);
}

export function listToolDefinitions(): ToolDefinitionSummary[] {
  return TOOL_DEFINITIONS.map((tool) => ({
    name: tool.name,
    category: tool.category,
    description: tool.description,
    annotations: ToolAnnotationsSchema.parse(tool.annotations),
    inputSchema: schemaToJson(tool.inputSchema, `${tool.name}Input`, {
      useRootRefs: tool.category === "artifact",
    }),
    outputSchema: schemaToJson(tool.outputSchema, `${tool.name}Output`, {
      useRootRefs: tool.category === "artifact",
    }),
  }));
}

export function getToolDefinition(name: string): MakoToolDefinition<string> | undefined {
  return TOOL_DEFINITION_MAP.get(name);
}

export function registerToolDefinition(definition: MakoToolDefinition<string>): void {
  TOOL_DEFINITION_MAP.set(definition.name, definition);
}

export function unregisterToolDefinition(name: string): void {
  if ((TOOL_DEFINITIONS as readonly MakoToolDefinition[]).some((tool) => tool.name === name)) {
    throw new Error(`Cannot unregister built-in tool: ${name}`);
  }

  TOOL_DEFINITION_MAP.delete(name);
}
