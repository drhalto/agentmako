# Mako AI for Cursor

This folder contains Mako skills plus Cursor MCP server wiring.

Install from the repo root:

```bash
mkdir -p ~/.cursor/plugins/local
ln -s "$(pwd)/plugins/cursor" ~/.cursor/plugins/local/mako-ai
```

The MCP server runs through `npx -y agentmako mcp`.
