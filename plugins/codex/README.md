# Mako AI for Codex

This plugin packages the same Mako skills and MCP server wiring used by the
Claude Code plugin.

Install from the repo root:

```bash
codex marketplace add ./plugins
```

The MCP server runs through `npx -y agentmako mcp`.
