# Mako AI for Gemini CLI

This extension packages Mako skills and MCP server wiring for Gemini CLI.

Install from the repo root:

```bash
gemini extensions install ./plugins/gemini
```

The MCP server runs through `npx -y agentmako mcp`.
